# .env.example — variables de entorno operativas (sin tokens, sin keys).
# Copiá este archivo a .env y ajustá los valores si hace falta.
# Importante: este repo NO requiere autenticación para descargar datos.

DATA_DIR=./data
MLFLOW_TRACKING_URI=sqlite:///./mlruns.db
PYTHONHASHSEED=42
